import moment from 'moment'

const constants = {
};

const initialState = {
    name: 'NORI',
	childname: 'FUKA'
}

function rootReducer(state = initialState,	action){
	switch(action.type) {
		default:
			return state
	}
}

export default rootReducer